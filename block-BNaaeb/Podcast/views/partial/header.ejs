<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>podcast</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <header class="flex">
        <a href="/" class="font-30-blue f-weight">Home</a>
       <div>
        <% if(user){ %>
            <a href="" class="font-20-light  margin-r-20"><%= user.name %></a>
            <a href="/users/logout" class="font-20-light  margin-r-20">logout</a>
            <a href="" class="font-20-light  margin-r-20 Profile">Profile</a>
            <ul>
                <li><%= user.name %></li>
                <li><%= user.email %></li>
                <li><%= user.subcription %></li>
            </ul>
            <% if(user.isAdmin){ %>
                <a href="/podcast/new" class="font-20-light ">Add podcast</a>
           <% } %>
        <% }else{ %>
            <a href="/users/login" class=" margin-r-20 font-20-light  ">Login</a>
            <a href="/users/registration" class="font-20-light ">Register</a>
        <% } %>
       </div>
        
    </header>
    
