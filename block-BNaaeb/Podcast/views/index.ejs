<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>podcast</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <header class="flex">
        <a href="" class="font-30-blue f-weight">PODCAST</a>
        <div>
            <% if(user){ %>
                <a href="" class="font-20-light  margin-r-20">
                    <%= user.name %>
                </a>
                <a href="/users/logout" class="font-20-light margin-r-20">logout</a>
                <a href="" class="font-20-light margin-r-20 Profile">Profile</a>
                <ul>
                    <li><%= user.name %></li>
                    <li><%= user.email %></li>
                    <li><%= user.subcription %></li>
                </ul>
                <% if(user.isAdmin){ %>
                    <a href="/podcast/new" class="font-20-light ">Add podcast</a>
                    <% } %>
                        <% }else{ %>
                            <a href="/users/login" class="font-20-light margin-r-20">Login</a>
                            <a href="/users/registration" class="font-20-light ">Register</a>
                            <% } %>
        </div>

    </header>
    <div class="flex-1">
        <h1 class="font-50-red-l f-weight margin-b-20">Podcast</h1>
        <a href="/podcast" class="btn f-weight">Go To Podcast</a>
    </div>
    <%- include('partial/footer') -%>